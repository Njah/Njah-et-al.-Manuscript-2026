---
title: "Number of hyper/hypomethylated  DMRS in genic regions"
author: "Relindis"
---

```{r setup, include=FALSE}
###Required Packages
library(devtools)
library(easyGgplot2)
library(readxl)
library(svglite)
```

```{r setup, include=FALSE}
DMRs_hyper_hypo_GE <- read_excel("DEd_DMRs_all_data_upregulation_for_histogram.xlsx")

DMRs_hyper_hypo_GE$Context <- factor(DMRs_hyper_hypo_GE$Context)

DMRs_hyper_hypo_GE$Context <- factor(DMRs_hyper_hypo_GE$Context,levels = unique(DMRs_hyper_hypo_GE$Context))
```
############Generate histogram plot for upregulated DEd DMRs upsteram of Alta############

#####Plot with numbers removed from histogram

```{r setup, include=FALSE}
#Alta Upstream upreg Set y limit to 0 and 400
Alta_upstream <- ggplot(DMRs_hyper_hypo_GE, aes(x=Context, y=Alta_upstream, fill=Methylation))+geom_bar(stat='identity')+ facet_wrap(~Tissue, scales="free_y", ncol = 1) +xlab('Alta')+ylab('Number of DMRs')+theme(panel.background = element_blank(),axis.text = element_text(size = 8, color = 'black'), text = element_text(size = 8),plot.title = element_text(hjust = 0.5))+ggtitle('Alta Upstream')+ ylim(0,400)

library(svglite)
ggsave('Alta_upstream.svg', width=3.0, height=3.0, plot=Alta_upstream, device = "svg", path="./", dpi=600)
```
